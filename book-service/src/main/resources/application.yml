server:
  port: 8021
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloudstudy
    username: root
    password: root
  application:
    name: book-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        # 将ephemeral修改为false,表示非临时实例
        ephemeral: false
        namespace: 8e7825e4-60b2-473c-afe0-2a13ae078a6e
    sentinel:
      transport:
        dashboard: localhost:8858
